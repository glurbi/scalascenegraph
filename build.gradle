apply plugin: 'scala'

sourceSets {
    main {
        scala {
            srcDir 'src'
        }
    }
}

repositories {
    mavenRepo urls: 'http://scala-tools.org/repo-releases/'
    flatDir dirs: 'lib/'
}

dependencies {
    scalaTools 'org.scala-lang:scala-compiler:2.8.0'
    scalaTools 'org.scala-lang:scala-library:2.8.0'
    compile 'org.scala-lang:scala-library:2.8.0'
    compile name: 'jogl.all'
    compile name: 'nativewindow.all'
    compile name: 'gluegen-rt'
    compile name: 'newt.all'
}
